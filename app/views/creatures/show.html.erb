<!-- app/views/creatures/show.html.erb -->

<h3><%= @creature.name %></h3>
<p><%= @creature.description %></p>
<p><%= image_tag @creature.image.url(:medium) %></p>
<%= button_to "Delete", @creature, method: :delete, data: { confirm: 'Are you sure?' } %>