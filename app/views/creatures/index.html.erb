<!DOCTYPE html>
<html lang="en">

<head>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Bog App</title>

    <!-- Bootstrap Core CSS -->
    <link href="css/bootstrap.min.css" rel="stylesheet">

    <!-- Custom CSS -->
    <link href="css/heroic-features.css" rel="stylesheet">

</head>

<body>

    <!-- Navigation -->
    <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
        <div class="container">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
              
            </div>
    </nav>

    <!-- Page Content -->
    <div class="container" style="width:800px; margin:0 auto;">

        <!-- Jumbotron Header -->
        <header class="jumbotron hero-spacer">
            <h1 align="center">The Bog </h1>
            <p align="center">A bog is a mire that accumulates peat, a deposit of dead plant material - such as mosses.</p>
            <img src="http://amytanathorn.com/wp-content/uploads/2014/11/moss-woods-sm.jpg" alt="forest moss">
        </header>
    </div>
        <hr>

        <!-- Page Features -->
    <div align="center">
        <h1>Latest Bogs</h1>
             <br>
            <table>

             <!-- creatures is the parameters to go through the loop. Similar to for each loop in JS. The block is telling us to go through the list of creatures -->
            <% @creatures.each do |creature| %>
              <tr>
                <td><%= creature.name %></td>
                <td><%= creature.description %></td>
                <td><%= image_tag creature.image.url(:medium) %></td>
                <td><%= link_to "Show", creature %></td>
                <td><%= link_to "Edit", edit_creature_path(creature) %></td>
                <td><%= link_to "Remove", creature, method: :delete, data: { confirm: "Are you sure?" } %></td>
              </tr>
            <% end %>
            </table>
 
        <br>
        <hr>
        <%= link_to "New", new_creature_path %>
    </div>

</body>

</html>